# Deferred follow-up: Righting adoption experience

Status: deferred — this is a capture of dogfood feedback, not a plan or implementation ticket.

## Concerns

- `npm link` is unreliable evidence of installation because global link state can affect the result. Dogfooding should validate an isolated project installing the packed npm artifact instead.
- Adoption still has too much mechanical ceremony: approved policy, additive ESLint configuration, native legacy-debt capture, and baseline verification are separate hand-edited steps.

## Resume point

When planning resumes, start fresh. No prior answers are recorded as decisions or constraints.

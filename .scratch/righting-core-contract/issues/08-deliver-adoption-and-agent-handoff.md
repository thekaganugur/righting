# Deliver the adoption and agent handoff

Type: task
Labels: wayfinder:task, rolling-delivery:waypoint
Status: resolved
Blocked by: 06

## Outcome

Maintainer and coding-agent adoption routes present the normalized contract, convention coverage, existing inconsistencies, and optional adapter activation as one coherent handoff without treating adoption as a source migration.

## Evidence

- Manual and agent-assisted onboarding distinguish incomplete policy shaping, explicit approval, normalized contract inspection, and optional adapter activation.
- `righting-integrate` hands the approved normalized contract unchanged to `righting-eslint`; existing inconsistencies remain visible as observed inconsistencies, become adapter findings only after verification, and become adapter-native legacy debt only after approved native suppression—not policy exceptions or migration work.
- Inspection, managed guidance, and packaged skills give coding agents the same contract semantics without claiming active guardrails or adapter evidence.
- Packed manual/agent-assisted journeys, package-documentation checks, the full test suite, and `git diff --check` pass.

## Delivery

Stage: delivered
Working context: unclaimed
Handoff: none

## Answer

Delivered the adoption and agent handoff:

- Manual and compatible-agent onboarding now separate incomplete policy shaping, exact maintainer approval, normalized-contract inspection, and optional adapter activation.
- Managed guidance directs coding agents to consume the normalized `contract` from `righting inspect --json` and keeps adapter activation explicitly unverified.
- `righting-integrate` reports integration-scan violations as observed inconsistencies, lists available guardrail adapters, recommends only compatible fits, and requires an explicit maintainer choice before handing the unchanged normalized contract to `righting-eslint`.
- Onboarding consistently distinguishes observed inconsistencies, verified adapter findings, approved adapter-native legacy debt, and active guardrails established by the project's successful normal lint command.
- Updated the dogfood managed block and packed manual/agent-assisted journey checks to prove the packaged handoff.

Validation:

- Packed manual-maintainer journey: passed.
- Packed compatible-agent journey: passed.
- Package-documentation checks: passed.
- `npm test`: 36 tests passed.
- `npm run typecheck`: passed.
- `git diff --check`: passed.
- Two-axis review: initial lifecycle and managed-guidance findings were corrected; the final standards and spec passes found no blockers.

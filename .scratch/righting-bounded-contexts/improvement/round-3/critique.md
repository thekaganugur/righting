## Review
- Correct: The replay follows the unapproved branch correctly: it labels the design `unresolved`, does not produce a firewall candidate or domain-document change, and asks one A/B/C/uncertain question (`replay.md:1-2,51-70`). This matches the approval and read-only gates in `SKILL.md:15-18,64-71`.
- Correct: Evidence is generally faithful and economical for this complex decision. It distinguishes design from realization evidence, retains `unknown` change authority, gives a conservative one-context alternative, and avoids duplicating the proposal cards under `Approved design` (`replay.md:19-49,54-55`). The local executable provenance is sufficient for this branch: it names the absolute project-local executable and its command (`replay.md:19`); a fresh direct inspection returned a valid policy, 76 covered files, no source violations, empty scopes, and inactive `context-firewall`.
- Correct: The capability limit is accurate: the installed policy language says scopes require approved context identities/relationships and the firewall checks static context/shared imports only (`node_modules/righting/docs/policy-language.md:90-95,121`; `replay.md:63-67`). Exact project terms such as *Product task*, *Reminder*, *New task alert*, *Device enrollment*, and *Official electronic tebligat* track `CONTEXT.md:5-23`.

- Note (P1 — reusable skill defect; fix now): `SKILL.md:54` requires a bidirectional relationship exchange and a producer/consumer trace, but does not distinguish an inter-context response from a separate external actor/service flow. That permits the replay’s incorrect compression of external parties and direction: it calls one endpoint “External UETS/Gmail signal” and calls “an authenticated task navigation” a return exchange from Attention Prompts to Follow-up Work (`replay.md:15-17,35,43,47`). The code instead sends an opaque destination outward with the push (`src/new-task-alert.manager.ts:104-111`; `src/worker.ts:81-99`); the browser service worker later navigates a window after a user click (`public/service-worker.js:14-34`). No Attention-Prompts-to-Follow-up-Work response data is evidenced. Gmail/IMAP transport, UETS authority, push service outcome, and browser/user navigation have distinct constraints and should not be silently merged.
  - Smallest general fix: append to the relationship rule in `SKILL.md:54`: “Name each external actor/service separately when it supplies distinct rules or a return flow. A user/browser action following a delivered link is an external-party flow, not a reverse context exchange, unless the receiving context consumes response data from the sending context.” This makes producer/consumer direction, external-party separation, and evidence fidelity predictable without adding a ledger or output section.
  - Replay-only correction: For this replay, record Follow-up Work → Attention Prompts as the translated eligibility/payload flow; record Attention Prompts → push service/device as send plus outcome; and record browser/user → authenticated Follow-up Work as navigation with no task mutation. Keep UETS authority separate from Gmail message transport. This is not a case for a candidate or a new boundary.

- Blocker: None. No other fix is worth doing now: approval readiness, executable provenance, output economy, vocabulary discipline, and read-only hygiene pass this replay.

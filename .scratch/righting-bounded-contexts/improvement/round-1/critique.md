## Review
- Correct: The replay correctly returns `unresolved` rather than treating `CONTEXT.md` or the existing role policy as bounded-context approval (`replay.md:19-23,74-80`). It keeps ownership unknown, supplies a conservative one-context alternative, asks one option-based question, and creates neither domain documents nor a real policy change. This follows the approval and read-only gates in `SKILL.md:18-21,40-61,71,90`.
- Correct: Evidence is generally faithful and separated into domain and realization evidence. The reported 76 covered files/no configured firewall is reproducible from `righting.json:1-40` and `npx --no-install righting inspect --json`; the latter reported 76 covered files, no source violations, and `context-firewall` not applied. The identified mixed seam is also supported by `src/access/tasks.access.ts` and composition in `src/worker.ts`.

- Blocker (rank 1) — reusable skill defect (strict read-only hygiene): The candidate procedure says to run `npx righting inspect --json` from a newly created mirror (`SKILL.md:88`) while the guardrail prohibits installation, relinking, and target-worktree temporary validation (`SKILL.md:21`). A source-only mirror has no local `node_modules`, so ordinary `npx` may download a package; copying a project with a file-linked Righting dependency can also be unresolved. This replay did not reach that branch, but this project demonstrates the risk: its dependency is `file:vendor/righting-0.1.0-569b9eb.tgz` (`package.json:59-77`) and `npm ls righting` reports it invalid. The validation command is therefore neither guaranteed to use the project's installed CLI nor guaranteed to stay non-installing. **Smallest general fix:** require an existing executable explicitly and invoke it by its absolute target-worktree path while `cwd` is the mirror (or use `npx --no-install` with an explicitly verified local prefix); if that executable is unavailable, fail gate 6 and return `omit-for-now`. Require `mktemp`/`finally` cleanup for the mirror as part of that same instruction.

- Note (rank 2) — reusable skill defect (predictable firewall evidence): The gate demands classification of “every relevant current source file” (`SKILL.md:80-84,88`) but never defines the source universe or requires the pre-gate normalized inspection that supplies it. “Relevant” can therefore drift between all repository code, configured coverage, tests, generated files, scripts, and out-of-coverage files. The replay happened to state a 76-file covered count (`replay.md:5`) because this repository's `AGENTS.md` independently requires inspection, not because the skill requires it. **Smallest general fix:** in the initial evidence pass, require a non-mutating installed-CLI inspection and record normalized coverage. At the firewall gate, enumerate the expansion of that coverage as the exhaustive classification universe and separately list source-like paths outside coverage as an explicit limitation/reconsideration item.

- Note (rank 3) — reusable skill defect (information hierarchy/sprawl): First-pass discovery is asked to stop once the highest-impact uncertainty is known (`SKILL.md:27`), yet the same always-loaded file carries the post-approval document-writing rules, six-gate candidate protocol, exact validation reporting, and fixed final template (`SKILL.md:63-127`). Those are distinct approval-state branches. This works, but it needlessly exposes post-completion material and helped produce a long first decision packet despite the instruction that it be concise (`SKILL.md:57`; `replay.md:4-90`). **Smallest general fix:** retain the initial evidence/recommendation/approval loop and the unresolved-result skeleton in `SKILL.md`; disclose recording and firewall-validation procedures through clearly named, approval-triggered reference files (also loaded for an already-approved-map replay).

- Note — replay execution mistake, not a reusable skill defect: The recommended relationship misstates the bidirectional handoff. It says Intake → Follow-up carries both notification metadata **and** the `created`/`duplicate`/`rejected` outcome, with Follow-up → Intake “none” (`replay.md:32-33,45-47`). In fact the connector sends the message and receives that outcome as the response: the app returns `result.outcome` after accepting/creating the task (`src/connector-message.manager.ts:50-69`), and the connector consumes `created`/`duplicate` from `post_message` (`scripts/home_uets_connector.py:200-205`). It should read: Intake → Follow-up: translated minimized notification metadata; Follow-up → Intake: `created`/`duplicate`/`rejected` acknowledgement. `SKILL.md:53` already explicitly requires separate exchange in each direction, so no skill change is needed.

- Note: No actionable duplication, no-op, or unsafe negation defect remains. The repeated prohibitions are safety/approval hard guardrails and are paired with positive actions (for example, classify source and validate a temporary candidate rather than edit the project). The model-invoked description names the actual discovery/reassessment and integration branches, so its context load is justified.

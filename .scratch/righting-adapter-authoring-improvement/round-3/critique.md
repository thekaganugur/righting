## Review

- **Correct:** The replay chooses the smaller package-activation path, separates current local activation from package conformance and proposed target activation, preserves the exact inspection capture, and correctly withholds target package activation (`replay.md:3-14,35-50,119-129`). I re-ran the focused package command: all 13 tests passed, and `npm run typecheck` passed. The current target facts also check out: the capture hashes to `bfb18a…`, there are 21 directives, and `npm ls` reports the symlinked `righting@0.1.0` invalid.

- **Blocker — replay/model miss (repeated artifact-identity failure):** The approved digest is not from the named immutable source revision. The replay admits it packed a worktree with a modified skill (`replay.md:9,16`). That modification is package material: `package.json:14-20` ships `skills/`, `npm pack --dry-run` includes `skills/write-righting-adapter/SKILL.md` at 10,131 bytes, while the same file at named revision `66d9c4e…` is 7,718 bytes. Thus SHA-256 `327e2c…` identifies the dirty-worktree payload, not the revision tree, and the instruction to recreate it from the revision while requiring the same digest (`replay.md:16,166-176`) is expected to fail. The claimed two reproductions only reproduce the mutable input, not the stated provenance. Fix by packing twice from two fresh materializations of one named revision after identical literal install/build steps, then approve the digest both runs produce. Because this failure recurred despite `SKILL.md:50` requiring revision + pack command + digest, the smallest sharper criterion is: **“For source-revision identity, pack twice from two fresh materializations of that revision after the same literal build steps; both digests must equal the approved digest.”** Replace/sharpen the existing identity sentence; do not add more narrative.

- **Blocker — replay/model miss, not a generalized wording gap:** The acceptance gate is still not wholly literal or sufficient to verify the artifact. `<empty-dir>` is an unresolved shell placeholder and therefore the package block is not copy-paste runnable (`replay.md:168-174`); it also runs in the mutable checkout rather than a materialization of `66d9c4e…`. The target block prints inspection and resolution but does not assert the captured SHA, the approved vendor SHA, or non-symlink installation (`replay.md:150-164`). It also does not check shipped `docs/oxlint.md` / `docs/adapter-conformance.md` or read the installed optional peer declaration. Replace the placeholder with literal `mktemp`/directory creation and hard-coded digest comparisons; add `test ! -L node_modules/righting`, an assertion that `require.resolve` is under the target’s `node_modules/righting`, checks for both shipped docs and `peerDependencies.oxlint === "1.75.0"`, and a literal hash comparison for `npx righting inspect --json`. These are already required by `SKILL.md:50,74`; the replay missed them.

- **Blocker — replay/model miss, not a generalized wording gap:** The passed and required ledgers are not yet honest for the one approved artifact. The package rows call the candidate immutable (`replay.md:85-102`), but the focused tests exercised `dist/src/oxlint.js` from the worktree, and `test/packed-oxlint.test.ts:12-79` packs a transient archive and deletes it without recording its digest. Those are valid source-worktree/package-behavior results, but they are not executed evidence for the falsely attributed `327e2c…` artifact. Mark them as source-worktree evidence and add a `required` final-artifact rerun with `observed: pending` until the clean artifact exists. Likewise, the target’s final row merely says “pending” (`replay.md:121-127`), rather than the required ledger form: named planned fixture/command, expected result, and literal `observed: pending`. The package `context-firewall` row also says “non-applicable to UETS” (`replay.md:100`); keep that row purely package-level as the executed unsupported-capability load rejection, and keep target non-applicability only in the target/support boundary. `SKILL.md:48` already states all of these distinctions precisely.

- **Fix — replay/model miss, not a generalized wording gap:** Make the target scope fully exact. It adds the new vendor archive but never explicitly deletes `vendor/righting-0.1.0-569b9eb.tgz` (`replay.md:133-144`). It also leaves direct target `micromatch` unspecified even though target `package.json:70` and the retiring local plugin are its only tracked non-lock uses. Name **Delete old archive** and **Remove direct `micromatch` from `package.json`/lock** (or explicitly justify retaining either). The existing exact add/change/delete requirement at `SKILL.md:48-50` is sufficient.

**Approval status:** not approval-ready until the artifact is rebuilt from the named immutable tree, its new digest replaces `327e2c…`, and the literal final-artifact/target gates and pending ledgers are corrected.

## Review

- **Correct:** No skill-level blocker remains. `skills/write-righting-adapter/SKILL.md:48-52,72-74` already requires immutable provenance, exact final-tree scope, machine assertions, separate evidence ledgers, and approval-now versus claim-later wording. The replay now honestly defers the support claim (`replay.md:3,94-101,145-149`) and its `rsync` consumer actually materializes the proposed working tree (`replay.md:125-140`).

- **Blocker — model execution, candidate provenance is not yet executable:** The digest is attributed to `b6aa8f7…` plus the test edit (`replay.md:34-52`), but the named Righting checkout is currently at `66d9c4e…`, and packed files changed after `b6aa8f7` (`src/cli.ts` and shipped skills). Applying the sole scoped edit at `replay.md:59-63` to that current tree does not produce the tree whose archive has the asserted `1a28c6…` digest. The pending candidate/artifact rows (`replay.md:98-99`) also have no literal command block: the commands begin only after the tarball already exists (`replay.md:103-110`). Name an explicit branch/worktree rooted at `b6aa8f7…`, apply and commit the repair there, assert that final SHA, then show the two fresh checkout/build/pack commands and literal digest comparisons. Otherwise the approved source scope cannot reproduce the approved artifact.

- **Blocker — model execution, the promised support record has no scoped destination:** `replay.md:101` requires appending the final candidate revision, archive digest, target revision, capture SHA, commands, exits, and limits, but the exact scope modifies only `test/init.test.ts` in Righting and deletes the dogfood-local record (`replay.md:59-76`). The commands merely test that packaged docs exist (`replay.md:117-119`). The shipped `docs/oxlint.md` already states a complete claim and that all gates passed (`docs/oxlint.md:52-63,77-91`), yet no named final evidence record can be updated under the proposed scope. Name the record and include its add/modify action plus machine verification. If the shipped document itself changes, the archive digest must be recomputed; if the final target ledger is separate, name that path explicitly and keep the shipped/package record and target-activation record distinct.

- **Blocker — dogfood/model execution, final commands still do not assert all promised identities:** `replay.md:107-122` checks the tar digest and public export, but not the literal `package.json` locator, the lockfile `resolved` value and integrity, the installed optional peer/version tuple, actual Oxlint runtime `1.75.0`, or a final inspection capture equal to `bfb18a…`. This matters because the reproduced defect is precisely that the installed tree did not match the lock-selected artifact (`replay.md:30`). Add failing assertions for those values (and the installed non-symlink/path identity) and regenerate/hash the final capture. Also assert the retained `respectEslintDisableDirectives:false`, which is promised at `replay.md:73,78` but omitted from the config assertion at line 115.

- **Blocker — dogfood scope remains non-minimal:** Deleting `tools/righting-oxlint-plugin.mjs` removes the only non-lock consumer of the target's direct `micromatch` dependency (`uets-to-task/tools/righting-oxlint-plugin.mjs:3,36-37`), but `micromatch` remains in `uets-to-task/package.json:70` and is absent from the scope table. Remove that direct dependency and regenerate the root lock metadata, or explicitly justify retaining it; Righting will still install its own exact `micromatch@4.0.8` dependency.

**Approval status:** the support claim is honestly deferred and there is no skill defect, but the implementation proposal is not yet approval-ready because its selected source tree, final evidence record, and acceptance assertions are not mutually executable as written.

## Review

- **Blocker — replay/model miss: there is no exact artifact to approve, so the approval request is internally unresolved.** `replay.md:79-80` leaves the archive filename, digest, and even release/version decision until later, `replay.md:100` requires substitution, and `replay.md:151` nevertheless calls the artifact digest-pinned. The named revision packages version `0.1.0`; choosing a version bump would produce a revision other than `b6aa8f7c6381a0a98934f6c5ec3b20ea3b583629`. The inspection capture also came from the mutable symlink admitted at `replay.md:55`, not the proposed artifact. This misses the explicit identity requirement in `skills/write-righting-adapter/SKILL.md:48-52`. Minimal fix: pack the unchanged `b6aa8f7…` candidate during assessment, retain/rename that one archive, record its exact filename, `npm pack --json`, SHA-512/SHA-256, and package version, then regenerate the target capture from a clean install of it. If a version bump is actually required, name the resulting revision and artifact instead. Do not leave that choice inside the approval. This remains the smallest reuse path; the verified 12/12 package tests support reusing `righting/oxlint` without changing `src/oxlint.ts`.

- **Blocker — replay/model miss: the acceptance sequence is neither copy-paste runnable nor sufficient artifact proof.** `replay.md:109` fails when `/tmp/righting-oxlint-release` does not already exist; I reproduced npm status 254/`ENOENT` for that exact missing pack destination. The sequence also omits repository `cd` commands, uses a multi-artifact wildcard (`*.tgz`), and contains unresolved substitutions. On the target side, `replay.md:116-119` checks only a declared version, non-symlink status, and prints hashes; it never compares the approved digest/integrity, proves lock resolution, checks the shipped support record, loads the public export, or verifies the actual Oxlint runtime. Regenerating the committed capture in place followed by `git diff --check` does not detect capture drift. Hard-code the one archive and expected hashes; create the destination; verify the lock/install, `docs/oxlint.md`, `docs/adapter-conformance.md`, `righting/oxlint`, and `oxlint --version`; and capture to a temporary file followed by `cmp` against the committed capture. Either install that retained archive in the promised clean consumer or explicitly use the clean target checkout as that consumer. These checks are already required by `SKILL.md:50,72-74`; no broader skill change is needed.

- **Blocker — replay/model miss: the ledger presents planned prose as if it were executable evidence and blends package conformance with target activation.** No row at `replay.md:126-135` has an actual `passed`/`required` status, named test case, observed exit, or executed command. In particular, `npm run lint` cannot create the synthetic 36-edge matrix claimed at `replay.md:128`; the proposed Vitest file would have to invoke those fixtures. `replay.md:131` places isolated variation/protected-capability policies in the target ledger even though `replay.md:137` then calls rejected capabilities package-suite evidence. Split this into (1) a package ledger whose `PASSED` rows cite the existing named `test/oxlint.test.ts` tests and observed 12-test command, and (2) a target ledger whose `REQUIRED` rows cite exact future test names, the exact UETS capture, the native command each test will invoke, expected exits/diagnostics, and `observed: pending`. Keep isolated unsupported-capability fixtures package-only. Also change `replay.md:33` to “five enabled/claimed role-dependency IDs”: the captured `contract.effective.policyRuleIds` actually contains eight IDs, including the three currently inapplicable scope IDs.

- **Fix — skill defect: the approval-ledger wording conflates future requirements with observations.** `SKILL.md:48` asks every row for “expected and observed exits” while also allowing status `required`; a pre-implementation required row cannot truthfully have an observed result. The round-2 replay filled that gap with “required observed” prose. Minimal generalized replacement: **“Use separate package-conformance and target-activation ledgers. A `passed` row names the executed artifact, fixture, public command, observed exit, and diagnostic. A `required` row names the planned fixture and command, records only expected results, and says `observed: pending`; planned fixtures are never evidence. Isolated capability policies are package evidence, not target activation.”**

- **Fix — dogfood-only: make the UETS file scope exact and minimal.** `replay.md:89` labels the new vendor archive as “Modify,” does not delete the obsolete `vendor/righting-0.1.0-569b9eb.tgz`, and leaves direct `micromatch` removal conditional. Repository search shows runtime use only in the local plugin being deleted (`tools/righting-oxlint-plugin.mjs:3,37`); the remaining occurrence in the adapter Markdown is prose, while packaged Righting supplies its own dependency. List **Add new archive**, **Delete old archive**, and **Remove direct `micromatch`** explicitly. No generalized skill text is needed because `SKILL.md:48-50` already requires exact add/change/delete scope.

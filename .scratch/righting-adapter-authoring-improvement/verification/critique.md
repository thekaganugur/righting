## Review

- **Correct:** The clean-source identity is now credible. I independently cloned revision `66d9c4e6a186521b5c61ed92f1bc80fa3e17a5cd`, ran the stated `npm ci --ignore-scripts` and `npm pack`, and reproduced SHA-256 `d63e0e09b72f904d7e19e419fa839800017a3f1a254f0fb5a9f3daaf8922db64`, size 42,029, and the stated SHA-512 integrity; the archive contains `dist/src/oxlint.js` and both shipped docs. The retained inspection file also reproduces `bfb18a…4e7b`. The replay honestly keeps final package/target gates pending (`replay.md:92-100`), chooses the smallest runtime path—activate the existing `righting/oxlint` export instead of owning another adapter (`replay.md:39-54`)—and does not turn the `/tmp` versus `/private/tmp` dogfood test failure into a universal skill rule. The additions to `SKILL.md:17,29,44,46-52,74-78` are generic provenance, reuse, approval, and final-tree criteria. **No skill-level blocker remains.**

- **Blocker — model miss (literal final-tree gate):** The clean-consumer command does not consume the proposed final tree. The scope says to run after uncommitted adds/changes/deletes (`replay.md:102-126`), but `git clone` at `replay.md:165` copies only the dogfood repository's committed `806a186…` tree. It will therefore restore the old vendor tarball, old lock, local plugin, test, and documentation—the exact state being replaced. Likewise, the package clones at `replay.md:140-143` are not explicitly checked out/asserted at `66d9c4e…`; after a commit they may pack a different tree, while before a commit they omit the proposed test repair, contrary to the “repaired source” wording at `replay.md:98`. Fix the package block by explicitly checking out and asserting `66d9c4e…` (and describe the unrelated repaired-worktree test gate separately), and make the isolated target consumer from the actual final working tree or require/name a final committed revision before cloning it. `SKILL.md:50,74` already requires exact artifact identity and final-tree commands, so this is not a skill defect.

- **Blocker — model miss (checks print instead of verify):** Several acceptance commands cannot fail when the promised identity is wrong. `shasum` and comments at `replay.md:145-146,156-157` do not assert either digest; `npx oxlint --version` at `replay.md:154` is not compared to `1.75.0`; and the intentional failure at `replay.md:169-170` checks only exit 1, not the required stable diagnostic. The gate also never asserts that `node_modules/righting` is non-symlinked or that resolution stays under the target installation, despite the current symlink being the reproduced root problem (`replay.md:40`). Replace these with literal equality/`grep` assertions, capture and assert the forbidden output, hash the copied vendor archive, and add `test ! -L node_modules/righting` plus an installed-path assertion. This is already demanded by `SKILL.md:50,60,72-74`; it is a replay miss.

- **Fix — dogfood-only scope/model miss:** Removing the local plugin leaves direct `micromatch` unused: target `package.json:70` declares it, and repository search finds its only non-lock consumer at `tools/righting-oxlint-plugin.mjs:3,37`, which the proposal deletes. Add removal of the direct dependency and corresponding root lock entry to `replay.md:108-122`, or explicitly justify retaining it. The packaged adapter will still bring its own exact `micromatch@4.0.8`; the target should not retain an orphan direct dependency.

- **Fix — model miss (approval wording):** `replay.md:3` says to approve the replacement only after the required ledger is green, while `replay.md:181-183` asks for approval to perform the replacement now and defers only the support claim. Make the distinction explicit: approve the implementation scope now; do not approve the complete support claim until every required row passes. As written, the opening can deadlock the approval branch in `SKILL.md:48-52`.

**Approval status:** not maintainer approval-ready until the final-tree consumer and machine-enforced identity/diagnostic checks are corrected. The evidence otherwise demonstrates a generic skill improvement, and the remaining defects are model/dogfood execution misses rather than missing universal skill guidance.
